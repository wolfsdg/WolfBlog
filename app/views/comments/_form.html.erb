
  <%= simple_form_for([@post,@post.comments.build], html: {class: "form-horizontal"}) do |f| %>
    <% if @post.errors.any? %>
      <% %>
    <% end %>

    <% if user_signed_in? %>
      <%= f.hidden_field :alias, :value => current_user.full_name %>
    <% else %>
      <%= f.hidden_field :alias, :value => "anonymous" %>
    <% end %> 
      <%= f.input :message %>
      
    <div class="form-actions">
      <%= f.button :submit, "Publish comment", class: "btn btn-primary" %>
    </div>
  <% end %>
